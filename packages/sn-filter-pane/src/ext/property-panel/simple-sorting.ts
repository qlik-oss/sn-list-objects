const simpleSorting = {
  // component: SimpleSorting,
  // classification: {
  //   section: 'sorting',
  //   tags: ['simple'],
  //   exclusive: true,
  // },
  // reset: (data, args) => {
  //   const { layout } = args;
  //   const childIds = layout.qChildList.qItems.map((item) => item.qInfo.qId);

  //   DimensionModel.getListLayout().then(() => {
  //     getListboxInfos(args.model.app, childIds).then((listboxInfos) => {
  //       const defaultSortCriterias = {
  //         qSortByState: 1,
  //         qSortByAscii: 1,
  //         qSortByNumeric: 1,
  //       };
  //       Object.keys(listboxInfos).forEach((id) => {
  //         util.setValue(listboxInfos[id].properties, 'qListObjectDef.qDef.qSortCriterias.0', defaultSortCriterias);
  //         listboxInfos[id].model.setProperties(listboxInfos[id].properties);
  //       });
  //     });
  //   });
  // },
};

export default simpleSorting;
